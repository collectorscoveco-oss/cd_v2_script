[package]
name = "sonardeck-studio"
version = "0.1.0"
description = "Native desktop shell for SonarDeck Studio"
authors = ["fla321mex", "Hermes Agent"]
edition = "2021"

[lib]
name = "sonardeck_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
